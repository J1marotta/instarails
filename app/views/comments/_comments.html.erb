<h1>	Photo Comments</h1>
<%= form_with(model: [@photo, comment], local: true) do |form| %>
  <% if @photo.comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@photo.comment.errors.count, "error") %> prohibited this photo from being saved:</h2>

      <ul>
      <% @photo.comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>



<% @comments.each do |comment|  %>
<li>
	<%= comment.content %>
</li>
<% end  %>


	<%= form_with(model: [@photo, @comment], local: true) do |form| %>

	<div class="form-group">
		<%= form.text_area :content, class: 'form-control' %>
	</div>

	<div class="form-group">
		<%= form.submit class: 'btn btn-primary'  %>
	</div>


<% end %>
<% end %>
