<h1>
	Photo Comments</h1>

<%= image_tag @photo.image_url %>

<%= @comments.each do |comment|  %>
<li>
	<%= comment.content %>
</li>
<% end  %>

<%= form_with(model: [@photo, @comments], local: true) do |form| %>

<div class="form-group">
	<%= form.label :content %>
	<%= form.text_area :content, class: 'form-control' %>
</div>

<div class="form-group">
	<%= form.submit class: 'btn btn-primary'  %>
</div>

<% end %>
